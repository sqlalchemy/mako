# project metadata is in pyproject.toml; this file remains only for
# configuration sections that are not part of the packaging build

[mako_testing]
module_base = ./test/templates/modules
template_base = ./test/templates/
